{
  "functions": {
    "api/**/*.py": {
      "runtime": "python3.12",
      "maxDuration": 10,
      "memory": 512
    }
  },
  "routes": [
    { "src": "^/(projects(?:/.*)?|contact|)$", "dest": "/api/main.py" }
  ]
}
